These are install scripts and system files to create a wifipi.

roroot/install - makes the system power loss tolerant by creating a read only root file system

netsetup - makes system sponsor an AP on wlan0 for config and puts data access on wlan1.  REQUIRES EXTRA HARDWARE

etc - the system files to be installed to autostart and powerfail safe the target system
